#ifndef TASK2_HPP
#define TASK2_HPP

/**
 * @file task2.hpp
 * @brief Заголовочный файл функций для задания 2.
 * @details Содержит объявления функций для удаления из массива всех элементов,
 * являющихся полными квадратами, без использования дополнительного массива.
 * Обращение к элементам массива выполняется с использованием указателя.
 * @author Андреев Глеб
 * @date 12.03.2026
 */

const int MAX_LEN_A = 100;

/**
 * @brief Проверяет, является ли число полным квадратом.
 * @details Отрицательные числа полными квадратами не считаются.
 * @param value Проверяемое число.
 * @return true, если число является полным квадратом, иначе false.
 * @author Андреев Глеб
 * @date 12.03.2026
 */
bool isSquare(int value);

/**
 * @brief Удаляет из массива все полные квадраты сдвигом.
 * @details Дополнительный массив не используется. Доступ к элементам выполняется
 * только через указатели.
 * @param A Массив целых чисел.
 * @param lenA Текущая длина массива; после обработки содержит новую длину.
 * @return true, если входные данные корректны, иначе false.
 * @author Андреев Глеб
 * @date 12.03.2026
 */
bool removeSquares(int *A, int &lenA);

#endif